
<input type="text" id="${id}" onChange="updateList(this.value, '${id}', '${sDataList}', '${setId}')"
  list="${dataList}" name="${name}" placeholder="${placeHolder ?: 'AutoComplete' }">
<datalist id="${dataList}"></datalist>


